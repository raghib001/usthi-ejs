let config = {
  debug: true,
  port: 8000,
  database: {
    host: "localhost",
    username: "root",
    password: "",
    database: "innovative_const_group",
    dialect: "mysql",
  },
};

module.exports = config;
